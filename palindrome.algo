function pal(str)
 {
    if 
       (str.length <=1)
         return "str est palindrome"
        else if 
        (str[0] <> str [str.length-1])
        return "str non palindrome"
        else 
        return pal(str.slace (0, str.length))
    end 
}
end